/*
 * SPDX-FileCopyrightText: 2021 Zextras <https://www.zextras.com>
 *
 * SPDX-License-Identifier: AGPL-3.0-only
 */
// import {
// 	setConversationsFlag,
// 	setConversationsRead,
// 	moveConversationToTrash
// } from './conversation-actions';

describe('Conversation Actions', () => {
	test('unset Conversations Flag', async () => {
		// 	const dispatch = jest.fn((arg) => arg);
		// 	const t = jest.fn((arg) => arg);
		// 	const result = setConversationsFlag(['123', '234'], true, t, dispatch);
		//
		// 	expect(result.icon).toEqual('FlagOutline');
		// 	expect(result.label).toEqual('action.unflag');
		// 	expect(result.click).toBeDefined();
		// 	result.click();
		// 	expect(dispatch).toHaveBeenCalledTimes(1);
	});
	//
	// test('set Conversations Flag', async () => {
	// 	const dispatch = jest.fn((arg) => arg);
	// 	const t = jest.fn((arg) => arg);
	// 	const result = setConversationsFlag(['123', '234'], false, t, dispatch);
	//
	// 	expect(result.icon).toEqual('Flag');
	// 	expect(result.label).toEqual('action.flag');
	// 	expect(result.click).toBeDefined();
	// 	result.click();
	// 	expect(dispatch).toHaveBeenCalledTimes(1);
	// });
	// test('set Conversations Unread', async () => {
	// 	const dispatch = jest.fn((arg) => Promise.resolve(arg));
	// 	const t = jest.fn((arg) => arg);
	// 	const result = setConversationsRead(['123', '234'], true, t, dispatch);
	//
	// 	expect(result.icon).toEqual('EmailOutline');
	// 	expect(result.label).toEqual('action.mark_as_unread');
	// 	expect(result.click).toBeDefined();
	// 	result.click();
	// 	expect(dispatch).toHaveBeenCalledTimes(1);
	// });
	//
	// test('set Conversations Read', async () => {
	// 	const dispatch = jest.fn((arg) => Promise.resolve(arg));
	// 	const t = jest.fn((arg) => arg);
	// 	const result = setConversationsRead(['123', '234'], false, t, dispatch);
	//
	// 	expect(result.icon).toEqual('EmailReadOutline');
	// 	expect(result.label).toEqual('action.mark_as_read');
	// 	expect(result.click).toBeDefined();
	// 	result.click();
	// 	expect(dispatch).toHaveBeenCalledTimes(1);
	// });
	//
	// test('move conversation to trash', async () => {
	// 	const dispatch = jest.fn((arg) => arg);
	// 	const t = jest.fn((arg) => arg);
	// 	const createSnackbar = jest.fn();
	// 	const result = moveConversationToTrash(['123', '234'], t, dispatch, createSnackbar);
	//
	// 	expect(result.icon).toEqual('Trash2Outline');
	// 	expect(result.label).toEqual('action.delete');
	// 	expect(result.click).toBeDefined();
	// 	// result.click();
	// 	// expect(dispatch).toHaveBeenCalledTimes(1);
	// });
});
